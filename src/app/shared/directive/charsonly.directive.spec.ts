import { CharsonlyDirective } from './charsonly.directive';

describe('CharsonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CharsonlyDirective();
    expect(directive).toBeTruthy();
  });
});
